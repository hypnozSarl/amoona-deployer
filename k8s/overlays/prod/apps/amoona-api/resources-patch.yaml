apiVersion: apps/v1
kind: Deployment
metadata:
  name: amoona-api
spec:
  template:
    spec:
      containers:
        - name: amoona-api
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2048Mi"
              cpu: "1000m"
