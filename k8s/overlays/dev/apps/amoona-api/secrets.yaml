apiVersion: v1
kind: Secret
metadata:
  name: amoona-api-secrets
  labels:
    app: amoona-api
    app.kubernetes.io/name: amoona-api
    environment: dev
type: Opaque
stringData:
  # Database
  DB_URL: "jdbc:postgresql://postgres.amoona-dev.svc.cluster.local:5432/amoona_db"
  DB_USER: "amoona"
  DB_PASSWORD: "dev-postgres-password"
  # Redis
  REDIS_HOST: "redis.amoona-dev.svc.cluster.local"
  REDIS_PORT: "6379"
  REDIS_PASSWORD: ""
  # MinIO
  MINIO_ENDPOINT: "http://minio.amoona-dev.svc.cluster.local:9000"
  MINIO_ACCESS_KEY: "minio-admin"
  MINIO_SECRET_KEY: "dev-minio-password"
  # JWT
  JWT_SECRET: "amoona-dev-jwt-secret-key-256-bits-minimum-required"
  # Logstash
  LOGSTASH_HOST: "logstash.amoona-dev.svc.cluster.local"
  LOGSTASH_PORT: "5000"
