apiVersion: apps/v1
kind: Deployment
metadata:
  name: amoona-api
spec:
  template:
    spec:
      containers:
        - name: amoona-api
          resources:
            requests:
              memory: "384Mi"
              cpu: "100m"
            limits:
              memory: "1024Mi"
              cpu: "500m"
