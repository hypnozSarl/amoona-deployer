apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - mtls-policy.yaml

# Note: Linkerd control plane should be installed via CLI:
# linkerd install --crds | kubectl apply -f -
# linkerd install | kubectl apply -f -
# linkerd viz install | kubectl apply -f -
