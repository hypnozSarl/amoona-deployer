apiVersion: v1
kind: Service
metadata:
  name: amoona-front
  labels:
    app: amoona-front
    app.kubernetes.io/name: amoona-front
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: amoona-front